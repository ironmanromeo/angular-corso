import { FiltraArrayPipe } from './filtra-array.pipe';

describe('FiltraArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
