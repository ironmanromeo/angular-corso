import { RiduciLunghezzaPipe } from './riduci-lunghezza.pipe';

describe('RiduciLunghezzaPipe', () => {
  it('create an instance', () => {
    const pipe = new RiduciLunghezzaPipe();
    expect(pipe).toBeTruthy();
  });
});
