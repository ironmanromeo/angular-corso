import { ApplicaColoreDirective } from './applica-colore.directive';

describe('ApplicaColoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicaColoreDirective();
    expect(directive).toBeTruthy();
  });
});
